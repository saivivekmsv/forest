import React from "react";
import {Header} from "../../components";
const Layout = (props) => {
    return (
        <div className="home-content">
            <Header />            
            
        </div>
    )
};

export default Layout;